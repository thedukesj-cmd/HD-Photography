import { PageHero } from "@/components/ui/page-hero"

export function NewsHero() {
  return (
    <PageHero
      eyebrow="Stay Updated"
      title="News & Events"
      description="Club announcements, exhibition openings, workshop schedules, member achievements, and more."
    />
  )
}